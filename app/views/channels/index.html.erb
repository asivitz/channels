<h1>select a channel</h1>

<hr/>

<div style="color:blue;">
	(debug list of all channels)
	<ul id="channels_list">
	    <% @channels.each do |channel| %>
	        <li><%= link_to channel.name, channel %></li>
	    <% end %>
	</ul>
</div>

<hr/>

<div style="color:blue;">
	(debug list of all users)
	<ul id="user_list">
	    <% @users.each do |user| %>
	        <li><%= user.alias %></li>
	    <% end %>
	</ul>
</div>
