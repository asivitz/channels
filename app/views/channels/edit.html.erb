<h1>Editing channel</h1>

<%= form_for(@channel) do |f| %>
  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @channel %> |
<%= link_to 'Back', channels_path %>
